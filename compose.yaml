services:
  app:
    build: .
    command: npm run dev
    environment:
      HOST: 0.0.0.0
      PORT: 8787
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./src:/app/src:ro
      - ./public:/app/public:ro
    init: true
